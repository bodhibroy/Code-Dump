/*bubble Sorting*/
#include <stdio.h>
#include <conio.h>
int sort(int *x,int first,int last);
void main()
{
	int n,*x,i;
	printf("\n\tEnter the number of element=");
   scanf("%d",&n);
   x=(int*)malloc(n*sizeof(int));
   for(i=0;i<n;i++)
   	scanf("%d",x+i);
   i=sort(x,0,(n-1));
}
int sort(int *x,int first,int last)
{
	int t=0;
   while(t=0)
   {

   }
}
